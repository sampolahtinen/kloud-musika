{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Debug",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/src/main.ts",
      "preLaunchTask": "build",
      "outFiles": ["${workspaceFolder}/.build/**/*.js"],
      "console": "integratedTerminal"
    }
  ]
}
